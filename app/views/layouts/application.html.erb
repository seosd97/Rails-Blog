<!DOCTYPE html>
<html lang="ko">
  <head>
    <title>SSD Tech Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8">
    <meta name="author", content="seosd97">
    <meta name="description", content="개발 블로그">
    <meta name="viewport", content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible", content="IE=Edge">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag    'http://fonts.googleapis.com/css?family=Nanum+Gothic%7CConsolas', media: 'all' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= yield :head %>
  </head>

  <body>
    <header class="layout-header">
      <% if is_admin_page? %>
        <a href="/admin" class="home-link">SSD's Blog [Admin]</a>
        
        <% if has_login? %>
          <%= render 'layouts/admin_layout_menu' %>
        <% end %>

      <% else %>
        <a href="/" class="home-link">SSD's Blog</a>
      <% end %>
    </header>

    <!-- TODO : 우측 상단에 알림 메세지 박스를 띄워 줘야 함. -->
    <span class="notification-block">
    </span>
    <!---->

    <div class="message-block-area"></div>

    <%= yield %>
  </body>
</html>
