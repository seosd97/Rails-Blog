<h1>Posts</h1>
<ul class="post_list">
    <% @posts.each do |post| %>
        <li>
            <%= link_to post.title, get_post_path(post)%>
        </li>
    <% end %>
</ul>

<% if is_admin_page? %>
    <%= button_to "New Post", { action: :new }, method: :get, class: "new_post_button" %>
<% end %>